export const Reservations = () => {
  return <div>Reservations</div>;
};

// fields, reservations, teams, tournaments, users

//dentro de cada directorio un directorio components

// y dentro de cada directorio components un archivo jsx con el mismo nombre
